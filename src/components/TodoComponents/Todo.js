import React from 'react'

//Step 8: build your Todo component
const Todo = props =>{
    //Step 9: return a div with an h4 that holds the ?????
    return (
        <div>
            <h4>{props.todo.task}</h4>
        </div>
    );
}

export default Todo;